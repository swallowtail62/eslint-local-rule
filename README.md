# Sample Node project using eslint with custom (local) rules

The custom rules are defined in `./eslint/rules`.

## Setup

```
npm ci
```

## Run eslint

```
npx eslint ./src
```
or
```
npm run lint
```
